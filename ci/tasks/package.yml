platform: linux

image_resource:
  type: docker-image
  source: {repository: niaquinto/gradle}

inputs:
  - name: resource-addcar

outputs:
  - name: resource-jar

run:
  path: ./resource-addcar/ci/scripts/package.sh

